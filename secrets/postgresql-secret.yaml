# Generated with: kubeseal --format yaml < postgresql-secret-plain.yaml > secrets/postgresql-secret.yaml
# Never commit the plain (*-plain.yaml) file.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: postgresql-secret
  namespace: ledger-x
spec:
  encryptedData:
    postgres-password: SEALED_VALUE_HERE
    password: SEALED_VALUE_HERE
  template:
    metadata:
      name: postgresql-secret
      namespace: ledger-x
